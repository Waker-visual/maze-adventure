# 验收演示流程

对应本次课设阶段验收公告：迷宫组（405 教室）与 AI 玩家组（407 教室）18:30 同步验收。
本文档按"验收现场要做什么、用什么命令/按钮、期望看到什么"整理，照着走即可。

## 0. 验收前准备（提前完成）

- [ ] `npm run install:all` 已执行过，`server/node_modules`、`client/node_modules` 都在。
- [ ] 把老师发的压缩包解压好，记下路径，例如：
  - 迷宫测试样例：`.../迷宫测试样例及测试方法/mazes/*.json`、`mazeTest.py`
  - BOSS 测试样例：`.../Boss战测试样例/boss_case_*.json`
  - AI 玩家测试用例：现场发放的 3 个迷宫 JSON（不含 BOSS 血量）
- [ ] 提前跑一遍 `node server/scripts/checkSubmission.js best_maze_design_组长名.json`，确认自己要交的迷宫是合法的，不要留到现场才发现问题。
- [ ] 后端、前端能正常启动：
  ```bash
  npm run server   # http://127.0.0.1:3001
  npm run client   # http://127.0.0.1:5173
  ```

---

## 1. 迷宫任务验收（连通性 / 唯一路径）

助教用 `mazeTest.py` 现场跑，检测起点到终点是否连通、是否唯一通路、有无孤立区域。我们只需要：

1. 把最终迷宫文件放在 `mazeTest.py` 能读到的路径下（脚本里 `file_path` 写死了文件名，需要改成对应文件名或把文件重命名成脚本预期的名字）。
2. 现场运行：
   ```bash
   python mazeTest.py
   ```
3. 期望输出：`迷宫中从起点到终点是否有唯一通路: True`，且没有"孤立区域"提示。

> 如果想提前自查，可以用我们自己的合法性脚本（见第 3 节），它包含了连通性 + 唯一通路 + 更多字段的检查，比 `mazeTest.py` 更全面。

---

## 2. BOSS 战任务验收

老师会发 `boss_case_1.json` ~ `boss_case_5.json`（每个文件是 `{"B":[...], "PlayerSkills":[...]}`）。

1. 现场运行我们的求解脚本（可以一次性把整个文件夹传进去）：
   ```bash
   node server/scripts/runBossCases.js "<压缩包里 Boss战测试样例 文件夹路径>" --out=./boss-results
   ```
2. 终端会依次打印每个用例、每场 BOSS 的：
   - 最少回合数
   - 技能序列（索引形式 `[3, 2, 1, ...]`，1-based，对应 `PlayerSkills` 里第几个技能）
   - 技能序列（id 形式 `skill-3 -> skill-2 -> skill-1`，便于对照）
   - **同一组内后一场 BOSS 会继承前一场结束时的技能冷却状态**（这是我们按课程 Q&A 修正过的逻辑，不是从头满状态开始）
3. `--out=./boss-results` 会把每个用例的结果另存成 `boss_case_N.result.json`，字段包含原始 `B`、原始 `PlayerSkills`、以及生成的技能序列——**把这三样直接输入到助教的测试系统**里即可。

> 如果助教是让你在网页上演示单个用例：把该用例的 `B`/`PlayerSkills` 粘进网页"迷宫编辑区"的 JSON 里，点工具栏「BOSS」按钮求解，或点「BOSS 实战」看带复活机制的完整推演。

---

## 3. 提交"最佳迷宫"（迷宫组专属环节，紧接第 1/2 步之后）

1. 在网页 `http://127.0.0.1:5173` 里把最终 15×15 迷宫准备好（可以用「生成」按钮 + 种子复现，也可以手改 JSON）。
2. 点工具栏「**合法性自检**」，确认没有报错项（格式 / 起终点 / 连通性 / 唯一通路 / 15×15 尺寸 / 资源与陷阱 / BOSS 参数含"至少一个无冷却技能"）。
   - 命令行等价操作（更适合批量/离线自查）：
     ```bash
     node server/scripts/checkSubmission.js best_maze_design_组长名.json
     ```
3. 在「组长姓名」输入框填好名字，点「**导出最终版**」——只有合法性自检通过才会真正导出，文件名自动生成为：
   ```
   best_maze_design_组长名.json
   ```
4. 现场提交"迷宫基准结果"时需要的四个数字，从「AI 调试」运行结果里读：
   - 点工具栏「AI 调试」（策略随意，建议用 `optimal`，即 DP 最优收集）。
   - **BOSS 战之前的资源值**：在结果 JSON 的 `events` 数组里，找 `type: "boss"` 那一条紧邻的**上一条**事件的 `resource` 字段。
   - **最终剩余资源价值**：结果里的 `remainingResource`。
   - **步数**：结果里的 `steps`。
   - **二者比值**：结果里的 `scoreRatio`（若 `success` 为 `false`，即未到达终点，该值会被强制记为 `0`，与验收规则一致）。
5. 助教现场合法性复检、提取 BOSS 血量、生成不含 BOSS 血量的正式交叉测试文件——这一步是助教操作，我们只需保证第 2 步自检通过，别无需要做的。

---

## 4. AI 玩家"完整迷宫探险任务"验收（如需现场演示 AI 玩家部分）

老师现场发 3 个测试用例，**迷宫 JSON 不含 BOSS 血量**，走到 BOSS 格子时要能通过键盘依次输入血量，输入 `-1` 结束该迷宫的 BOSS 输入。

1. 对每个测试用例运行：
   ```bash
   node server/scripts/runAiCli.js <发放的迷宫.json> --strategy=optimal
   ```
2. 终端提示"检测到迷宫中有 N 个 BOSS 格子，本次迷宫数据不含 BOSS 血量"后，按提示逐个输入 BOSS 血量：
   ```
   请输入 BOSS 血量（第 1 个，输入 -1 结束）: 11
   请输入 BOSS 血量（第 2 个，输入 -1 结束）: 13
   请输入 BOSS 血量（第 3 个，输入 -1 结束）: 9
   请输入 BOSS 血量（第 4 个，输入 -1 结束）: 15
   请输入 BOSS 血量（第 5 个，输入 -1 结束）: -1
   ```
3. 输入完 `-1` 后脚本自动完成整场探险，直接打印验收需要的核心指标：
   ```
   是否到达终点: 是/否
   移动步数: N
   剩余金币（资源）数: N
   验收指标（剩余资源/步数，失败记 0）: N
   ```
   这一行「验收指标」就是公告里说的核心指标，失败时脚本已经自动记成 `0`，不用再手算。

---

## 5. 应急预案

| 情况 | 处理方式 |
| --- | --- |
| 端口被占用 / 网页打不开 | 全程用命令行脚本（第 2、3、4 节都有命令行等价方式），不依赖网页也能完成验收 |
| `python mazeTest.py` 提示找不到文件 | 把待测迷宫 JSON 复制到 `mazeTest.py` 所在目录，或按脚本里的 `file_path` 改文件名 |
| 合法性自检没过 | 看终端/网页打印的具体错误项逐条修（最常见：忘了给 15×15、PlayerSkills 缺无冷却技能） |
| 现场要看某个 BOSS 用例的详细搜索过程 | `node server/scripts/runBossCases.js <单个case.json>`，不加 `--out` 只看终端输出即可 |
