# 迷宫探险项目运行配置说明

这份文档用于把项目分享给同事后，快速完成本地安装、运行、测试与常见问题排查。

## 1. 环境要求

- Node.js：建议使用 Node.js 18 或更高版本。
- npm：随 Node.js 一起安装即可。
- 操作系统：Windows、macOS、Linux 均可运行。

检查版本：

```bash
node -v
npm -v
```

## 2. 项目结构

```text
maze-adventure/
  client/   Vue 3 + Vite 前端
  server/   Node.js + Express 后端与算法 API
```

默认端口：

| 服务 | 默认地址 | 说明 |
| --- | --- | --- |
| 前端 | `http://127.0.0.1:5173` | Vite 开发服务器 |
| 后端 | `http://127.0.0.1:3001` | Express API 服务 |

前端请求 `/api/*` 时，会通过 `client/vite.config.js` 代理到 `http://127.0.0.1:3001`。

## 3. 首次安装

在项目根目录执行：

```bash
npm run install:all
```

该命令会分别安装后端和前端依赖：

```bash
npm --prefix server install
npm --prefix client install
```

## 4. 启动项目

需要打开两个终端窗口。

终端 1：启动后端 API。

```bash
npm run server
```

看到类似输出即表示后端启动成功：

```text
Maze Adventure API running at http://localhost:3001
```

终端 2：启动前端页面。

```bash
npm run client
```

然后在浏览器访问：

```text
http://127.0.0.1:5173
```

## 5. 运行测试

在项目根目录执行：

```bash
npm test
```

测试覆盖内容包括：

- 四种迷宫生成算法的连通性与唯一路径校验。
- 随机种子复现。
- 动态规划资源路径。
- BOSS 分支限界求解与复活推演。
- 贪心 3x3 资源拾取。
- AI 玩家策略模拟。
- 迷宫 x AI 交叉测试矩阵。

## 6. 生产构建

如需构建前端静态文件：

```bash
npm --prefix client run build
```

构建产物位于：

```text
client/dist/
```

本项目后端当前没有托管 `client/dist` 的静态服务逻辑。如果要部署到服务器，可以采用以下方式之一：

- 前端使用 Nginx、静态文件服务或平台托管 `client/dist`。
- 后端单独运行 Express API。
- 生产环境中将前端 `/api` 请求反向代理到后端服务地址。

## 7. 后端端口配置

后端默认监听 `3001`，也可以通过环境变量 `PORT` 修改。

Windows PowerShell：

```powershell
$env:PORT=3002
npm --prefix server run dev
```

macOS / Linux：

```bash
PORT=3002 npm --prefix server run dev
```

如果修改了后端端口，也要同步修改 `client/vite.config.js` 中的代理地址：

```js
proxy: {
  '/api': 'http://127.0.0.1:3002'
}
```

## 8. 常见问题

### PowerShell 提示无法加载 npm.ps1

如果在 Windows PowerShell 中运行 `npm` 出现类似错误：

```text
无法加载文件 ... npm.ps1，因为在此系统上禁止运行脚本
```

可以改用：

```powershell
npm.cmd run install:all
npm.cmd run server
npm.cmd run client
```

也可以在 CMD 终端中运行普通 `npm` 命令。

### 前端页面能打开，但按钮请求失败

通常是后端没有启动，或端口不一致。

检查：

- 后端是否已运行在 `http://127.0.0.1:3001`。
- `client/vite.config.js` 中 `/api` 代理地址是否正确。
- 浏览器控制台或终端是否有 API 报错。

### 端口被占用

如果 `3001` 或 `5173` 被占用：

- 后端：通过 `PORT` 环境变量修改。
- 前端：修改 `client/vite.config.js` 中的 `server.port`。

修改前端端口示例：

```js
server: {
  port: 5174,
  proxy: {
    '/api': 'http://127.0.0.1:3001'
  }
}
```

### 依赖安装失败或版本异常

建议删除对应目录下的 `node_modules` 后重新安装：

```bash
npm --prefix server install
npm --prefix client install
```

如果网络环境较慢，可以配置 npm 镜像源后再安装。

## 9. 主要 API 端点

| 方法 | 路径 | 功能 |
| --- | --- | --- |
| GET | `/api/sample` | 获取样例迷宫 |
| POST | `/api/maze/generate` | 生成迷宫 |
| POST | `/api/maze/validate` | 校验迷宫 |
| POST | `/api/maze/compare` | 四算法对比 |
| GET | `/api/algorithm/info` | 获取算法复杂度说明 |
| POST | `/api/resource/optimal-path` | 动态规划资源路径 |
| POST | `/api/boss/solve` | BOSS 最少回合与技能序列 |
| POST | `/api/boss/battle` | BOSS 实战复活推演 |
| POST | `/api/ai/simulate` | AI 玩家完整探险模拟 |
| POST | `/api/ai/greedy-step` | 贪心 3x3 单步决策 |
| POST | `/api/ai/greedy-benchmark` | 贪心多用例评测 |
| POST | `/api/cross-test` | 迷宫 x AI 交叉测试 |
| POST | `/api/maze/legality` | 提交前合法性自检（格式/起终点/连通性/尺寸/资源与陷阱/BOSS 参数） |

## 10.1 现场验收 CLI 脚本

```bash
# BOSS 战验收：批量跑 boss_case_*.json，打印并导出每个用例每场 BOSS 的技能序列
node server/scripts/runBossCases.js <case.json 或目录> [...更多路径] [--out=结果目录]

# 提交前自检 best_maze_design_组长名.json
node server/scripts/checkSubmission.js <best_maze_design_xxx.json>

# AI 玩家现场验收：迷宫不含 BOSS 血量，到 BOSS 位置时按提示输入血量（-1 结束）
node server/scripts/runAiCli.js <maze.json> [--strategy=greedy|optimal|speedrun]
```

## 10. 推荐分享方式

分享给同事时建议包含：

- 项目完整目录。
- `package.json`、`package-lock.json`。
- `client/` 和 `server/` 目录。
- `README.md`。
- 本文档 `运行配置说明.md`。

不建议手动删除 `package-lock.json`。它可以帮助同事安装到更一致的依赖版本。
